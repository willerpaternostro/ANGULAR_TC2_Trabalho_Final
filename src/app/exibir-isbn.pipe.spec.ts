import { ExibirISBNPipe } from './exibir-isbn.pipe';

describe('ExibirISBNPipe', () => {
  it('create an instance', () => {
    const pipe = new ExibirISBNPipe();
    expect(pipe).toBeTruthy();
  });
});
